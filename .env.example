# Chiave per il proxy AI (netlify/functions/ai.ts) — stessa GEMINI_API_KEY
# del progetto dude_images_generator. Non committare mai il file .env reale.
# Su Netlify: Site settings → Environment variables.
GEMINI_API_KEY=
